# ==========================================================================
# Aggregate requirements for running the whole project on one machine.
# For containerized / per-service installs, use each service's own
# requirements.txt (sensor-service/, stream-processing/, frontend-dashboard/).
# ==========================================================================

# --- Sensor service (simulator + API) ---
kafka-python==2.0.2
fastapi==0.110.0
uvicorn[standard]==0.29.0
pydantic==2.6.4

# --- Stream processing (Spark + AI) ---
pyspark==3.5.0
scikit-learn==1.4.1.post1
numpy==1.26.4
tensorflow==2.16.1   # real LSTM temperature forecast (train_lstm.py)

# --- Dashboard ---
streamlit==1.33.0
pandas==2.2.1
cassandra-driver==3.29.1
requests==2.31.0
