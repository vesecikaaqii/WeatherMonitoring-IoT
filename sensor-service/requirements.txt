# Sensor service (simulator + FastAPI control plane)
kafka-python==2.0.2
fastapi==0.110.0
uvicorn[standard]==0.29.0
pydantic==2.6.4
# Used by the stress test to persist a summary row to performance_metrics
cassandra-driver==3.29.1
