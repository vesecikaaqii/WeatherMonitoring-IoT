# Stream processing (Spark + AI)
pyspark==3.5.0
# AI anomaly detection (optional but recommended; falls back if missing)
scikit-learn==1.4.1.post1
numpy==1.26.4
# LSTM temperature forecast (train_lstm.py + lstm_forecast.py).
# Falls back to moving average if missing.
tensorflow==2.16.1
