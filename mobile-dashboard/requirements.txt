# Mobile dashboard (FastAPI backend + PWA frontend)
fastapi==0.110.0
uvicorn[standard]==0.29.0
cassandra-driver==3.29.1
requests==2.31.0
# LSTM temperature forecast: load the trained Keras model and predict.
# If these are removed, the forecast falls back to a moving average.
numpy==1.26.4
tensorflow==2.16.1
